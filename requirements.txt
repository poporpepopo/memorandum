openai-whisper
ollama
SpeechRecognition
plyer
numpy
PyAudio

# Windows only: modern toast notifications (plyer's balloon API is often hidden on Windows 11)
winotify; sys_platform == "win32"

# Windows only: WASAPI loopback capture for --source system (web meetings)
PyAudioWPatch; sys_platform == "win32"
